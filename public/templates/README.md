# Variance Workbook Template

Place the production variance workbook template at:

`public/templates/variance-template.xlsx`

The server-side variance generator loads this bundled repo path in production/Vercel. If the file is missing, the app falls back to a generated workbook with `Instructions` and `Template` sheets.
